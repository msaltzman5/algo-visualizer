{
  "name": "algo-visualizer",
  "version": "1.0.0",
  "description": "Desktop algorithm visualizer",
  "main": "main.js",
  "scripts": {
    "start": "node -e \"const { spawn } = require('child_process'); const env = { ...process.env, ELECTRON_DISABLE_SANDBOX: '1' }; const child = spawn('electron', ['.'], { stdio: 'inherit', env }); child.on('exit', code => process.exit(code ?? 0));\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^39.2.4"
  }
}
